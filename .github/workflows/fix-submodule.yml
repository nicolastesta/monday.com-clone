{"name":"Fix broken submodule","on":"workflow_dispatch","jobs":{"fix":{"runs-on":"ubuntu-latest","permissions":{"contents":"write"},"steps":[{"uses":"actions/checkout@v4","with":{"submodules":false,"fetch-depth":0}},{"name":"Remove broken submodule and push","run":"git config user.name 'github-actions[bot]' && git config user.email 'github-actions[bot]@users.noreply.github.com' && git rm -r --cached TEAM-SUNDAY || true && rm -rf TEAM-SUNDAY && git add -A && git commit -m 'Remove broken submodule reference' || echo 'nothing to commit' && git push"}]}}}
