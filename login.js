 const form = document.querySelector("form"); form.addEventListener("submit", checkData); async function checkData(event) { event.preventDefault(); const mail = document.getElementById("email").value; const password = document.getElementById("password").value; const result = await supabaseClient.auth.signInWithPassword({ email: mail, password: password }); if (result.error) { alert("Login failed: " + result.error.message); } else { window.location.href = "user.html"; } }
